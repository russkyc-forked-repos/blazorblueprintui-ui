@page "/docs/getting-started"

<PageTitle>Getting Started - BlazorUI</PageTitle>

<div class="container py-8 md:py-12 px-4">
    <div class="mx-auto max-w-4xl">
        <div class="space-y-4 mb-8">
            <h1 class="text-4xl font-bold tracking-tight">Getting Started</h1>
            <p class="text-xl text-muted-foreground">
                Learn how to use BlazorUI components in your Blazor application.
            </p>
        </div>

        <Separator class="my-8" />

        <div class="space-y-6">
            <h2 class="text-2xl font-bold">Using Components</h2>
            <p>
                BlazorUI provides two types of components: <strong>Primitives</strong> (unstyled) and <strong>Components</strong> (styled).
                You can use either depending on your needs.
            </p>

            <h3 class="text-xl font-semibold">Using Styled Components</h3>
            <p>
                Styled components come with beautiful defaults and are ready to use immediately.
                Here's a simple example using the Button component:
            </p>

            <div class="not-prose my-6">
                <Card>
                    <CardHeader>
                        <CardTitle>Button Example</CardTitle>
                    </CardHeader>
                    <CardContent>
                        <div class="space-y-4">
                            <div class="rounded-md bg-muted p-4 font-mono text-sm overflow-x-auto">
                                <pre class="text-xs">@@page "/example"

&lt;Button Variant="ButtonVariant.Default" OnClick="HandleClick"&gt;
    Click me
&lt;/Button&gt;

@@code {
    private void HandleClick()
    {
        // Handle button click
    }
}</pre>
                            </div>
                            <div class="flex gap-2 p-4 border rounded-md">
                                <Button Variant="ButtonVariant.Default">Click me</Button>
                            </div>
                        </div>
                    </CardContent>
                </Card>
            </div>

            <h3 class="text-xl font-semibold">Component Variants</h3>
            <p>
                Most components support different variants, sizes, and states. Here's an example with Button:
            </p>

            <div class="not-prose my-6">
                <Card>
                    <CardHeader>
                        <CardTitle>Button Variants</CardTitle>
                    </CardHeader>
                    <CardContent>
                        <div class="space-y-4">
                            <div class="rounded-md bg-muted p-4 font-mono text-sm overflow-x-auto">
                                <pre class="text-xs">&lt;Button Variant="ButtonVariant.Default"&gt;Default&lt;/Button&gt;
&lt;Button Variant="ButtonVariant.Secondary"&gt;Secondary&lt;/Button&gt;
&lt;Button Variant="ButtonVariant.Outline"&gt;Outline&lt;/Button&gt;
&lt;Button Variant="ButtonVariant.Ghost"&gt;Ghost&lt;/Button&gt;
&lt;Button Variant="ButtonVariant.Link"&gt;Link&lt;/Button&gt;</pre>
                            </div>
                            <div class="flex flex-wrap gap-2 p-4 border rounded-md">
                                <Button Variant="ButtonVariant.Default">Default</Button>
                                <Button Variant="ButtonVariant.Secondary">Secondary</Button>
                                <Button Variant="ButtonVariant.Outline">Outline</Button>
                                <Button Variant="ButtonVariant.Ghost">Ghost</Button>
                                <Button Variant="ButtonVariant.Link">Link</Button>
                            </div>
                        </div>
                    </CardContent>
                </Card>
            </div>

            <h3 class="text-xl font-semibold">Using Cards</h3>
            <p>
                Cards are a common pattern for organizing content. Here's how to use the Card component:
            </p>

            <div class="not-prose my-6">
                <Card>
                    <CardHeader>
                        <CardTitle>Card Example</CardTitle>
                    </CardHeader>
                    <CardContent>
                        <div class="space-y-4">
                            <div class="rounded-md bg-muted p-4 font-mono text-sm overflow-x-auto">
                                <pre class="text-xs">&lt;Card&gt;
    &lt;CardHeader&gt;
        &lt;CardTitle&gt;Card Title&lt;/CardTitle&gt;
        &lt;CardDescription&gt;Card description goes here&lt;/CardDescription&gt;
    &lt;/CardHeader&gt;
    &lt;CardContent&gt;
        &lt;p&gt;Card content goes here.&lt;/p&gt;
    &lt;/CardContent&gt;
    &lt;CardFooter&gt;
        &lt;Button Variant="ButtonVariant.Default"&gt;Action&lt;/Button&gt;
    &lt;/CardFooter&gt;
&lt;/Card&gt;</pre>
                            </div>
                            <div class="p-4 border rounded-md">
                                <Card class="max-w-md">
                                    <CardHeader>
                                        <CardTitle>Card Title</CardTitle>
                                        <CardDescription>Card description goes here</CardDescription>
                                    </CardHeader>
                                    <CardContent>
                                        <p>Card content goes here.</p>
                                    </CardContent>
                                    <CardFooter>
                                        <Button Variant="ButtonVariant.Default">Action</Button>
                                    </CardFooter>
                                </Card>
                            </div>
                        </div>
                    </CardContent>
                </Card>
            </div>

            <h2 class="text-2xl font-bold">Using Primitives</h2>
            <p>
                Primitives are unstyled components that give you full control over the visual design.
                They handle accessibility, keyboard navigation, and state management while you provide the styling.
            </p>

            <div class="not-prose my-6">
                <Card>
                    <CardHeader>
                        <CardTitle>Primitive Example</CardTitle>
                    </CardHeader>
                    <CardContent>
                        <div class="rounded-md bg-muted p-4 font-mono text-sm overflow-x-auto">
                            <pre class="text-xs">@@using BlazorUI.Primitives.Primitives

&lt;Dialog&gt;
    &lt;DialogTrigger class="px-4 py-2 bg-blue-500 text-white rounded"&gt;
        Open Dialog
    &lt;/DialogTrigger&gt;
    &lt;DialogContent class="bg-white p-6 rounded-lg shadow-lg"&gt;
        &lt;DialogHeader&gt;
            &lt;DialogTitle class="text-xl font-bold"&gt;Dialog Title&lt;/DialogTitle&gt;
        &lt;/DialogHeader&gt;
        &lt;p&gt;Dialog content goes here.&lt;/p&gt;
    &lt;/DialogContent&gt;
&lt;/Dialog&gt;</pre>
                        </div>
                    </CardContent>
                </Card>
            </div>

            <h2 class="text-2xl font-bold">Theming</h2>
            <p>
                BlazorUI uses CSS variables for theming, making it easy to customize colors and styles.
                You can modify the theme by updating the CSS variables in your stylesheet:
            </p>

            <div class="not-prose my-6">
                <Card>
                    <CardHeader>
                        <CardTitle>Theme Variables</CardTitle>
                    </CardHeader>
                    <CardContent>
                        <div class="rounded-md bg-muted p-4 font-mono text-sm overflow-x-auto">
                            <pre class="text-xs">:root {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
  --primary: 222.2 47.4% 11.2%;
  --primary-foreground: 210 40% 98%;
  /* ... more variables */
}

.dark {
  --background: 222.2 84% 4.9%;
  --foreground: 210 40% 98%;
  /* ... dark mode overrides */
}</pre>
                        </div>
                    </CardContent>
                </Card>
            </div>

            <h2 class="text-2xl font-bold">Next Steps</h2>
            <p>
                Now that you know the basics, explore the component library to see what's available:
            </p>

            <ul class="space-y-2 list-disc list-inside">
                <li><a href="/components">Browse Components</a> - See all styled components with examples</li>
                <li><a href="/primitives">Browse Primitives</a> - Explore unstyled primitives for custom designs</li>
                <li><a href="https://github.com/blazorui-net/ui" target="_blank" rel="noopener noreferrer">GitHub Repository</a> - View source code and contribute</li>
            </ul>

            <div class="flex gap-3 mt-6">
                <Button Variant="ButtonVariant.Default" OnClick="@(() => NavigationManager.NavigateTo("/components"))">
                    Browse Components
                </Button>
                <Button Variant="ButtonVariant.Outline" OnClick="@(() => NavigationManager.NavigateTo("/primitives"))">
                    Browse Primitives
                </Button>
            </div>
        </div>
    </div>
</div>

@code {
    [Inject] private NavigationManager NavigationManager { get; set; } = default!;
}
