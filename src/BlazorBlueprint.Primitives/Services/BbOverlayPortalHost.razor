@namespace BlazorBlueprint.Primitives.Services

@* Renders only Overlay portals (Popover, Select, Dropdown, Tooltip, HoverCard).
   Use alongside BbContainerPortalHost for category-scoped rendering,
   or use BbPortalHost for both categories in one component. *@
<BbCategoryPortalHost Category="PortalCategory.Overlay" />
