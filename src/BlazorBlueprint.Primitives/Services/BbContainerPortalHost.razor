@namespace BlazorBlueprint.Primitives.Services

@* Renders only Container portals (Dialog, Sheet, AlertDialog).
   Use alongside BbOverlayPortalHost for category-scoped rendering,
   or use BbPortalHost for both categories in one component. *@
<BbCategoryPortalHost Category="PortalCategory.Container" />
