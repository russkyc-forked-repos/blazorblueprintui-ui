@using BlazorBlueprint.Primitives.Select

<BbSelect TValue="string" @bind-Value="value"
        ItemClass="px-3 py-2 cursor-pointer hover:bg-accent">
    <BbSelectTrigger class="...">
        <BbSelectValue Placeholder="Select a fruit..." />
        <span>&#9660;</span>
    </BbSelectTrigger>
    <BbSelectContent class="...">
        <BbSelectItem Value="@("apple")" Text="Apple" />
        <BbSelectItem Value="@("banana")" Text="Banana" />
    </BbSelectContent>
</BbSelect>

@* Value is the data value (hidden). Text is the display text shown in trigger.
   ItemClass applies shared styling to all items. *@