<BbTreeView TItem="DragNode"
            Items="@items"
            ChildrenProperty="@(n => n.Children)"
            TextField="@(n => n.Name)"
            ValueField="@(n => n.Id)"
            IconField="@(n => n.Icon)"
            Draggable="true"
            OnNodeDrop="HandleDrop"
            DefaultExpandAll="true" />

@code {
    private Task HandleDrop(TreeDropEventArgs<DragNode> args)
    {
        // args.SourceNode — the dragged node
        // args.TargetNode — the drop target
        // args.Position — Before, After, or Inside
        return Task.CompletedTask;
    }
}