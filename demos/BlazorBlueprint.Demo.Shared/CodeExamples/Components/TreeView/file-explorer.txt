<BbTreeView TItem="FileNode"
            Items="@fileData"
            ChildrenProperty="@(n => n.Children)"
            TextField="@(n => n.Name)"
            ValueField="@(n => n.Id)"
            IconField="@(n => n.Icon)"
            ShowSearch="true"
            SearchPlaceholder="Search files..."
            SelectionMode="TreeSelectionMode.Single"
            @bind-SelectedValue="selectedFile"
            DefaultExpandDepth="2"
            ShowLines="true" />