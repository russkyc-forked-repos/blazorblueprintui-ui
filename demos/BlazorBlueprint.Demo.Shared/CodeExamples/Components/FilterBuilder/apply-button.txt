<BbFilterBuilder @bind-Filter="filter"
                 Fields="@fields"
                 ShowApplyButton="true"
                 OnFilterChanged="HandleApplyFilter" />

@code {
    private void HandleApplyFilter(FilterDefinition filter)
    {
        // Only called when the user clicks "Apply Filter"
    }
}