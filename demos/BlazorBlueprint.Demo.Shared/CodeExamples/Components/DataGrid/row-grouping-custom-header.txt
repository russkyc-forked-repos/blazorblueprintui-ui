<BbDataGrid TData="Person" Items="@people" InitialPageSize="50">
    <Columns>
        <BbDataGridGroupColumn TProperty="string" Property="@(p => p.Status)">
            <HeaderTemplate>
                <div class="flex items-center gap-3">
                    <button type="button"
                            class="inline-flex items-center justify-center rounded-md w-6 h-6
                                   hover:bg-muted transition-colors"
                            @onclick="context.ToggleCollapse"
                            @onclick:stopPropagation="true">
                        <LucideIcon Name="@(context.IsCollapsed ? "chevron-right" : "chevron-down")"
                                    Class="w-4 h-4" />
                    </button>
                    <BbBadge Variant="@(context.Group.Key?.ToString() == "Active"
                        ? BadgeVariant.Default : BadgeVariant.Secondary)">
                        @context.Group.Key
                    </BbBadge>
                    <span class="text-muted-foreground text-sm">
                        @context.Group.ItemCount employees
                    </span>
                </div>
            </HeaderTemplate>
        </BbDataGridGroupColumn>
        <BbDataGridPropertyColumn Property="@(p => p.Name)" Sortable />
        <BbDataGridPropertyColumn Property="@(p => p.Department)" />
        <BbDataGridPropertyColumn Property="@(p => p.Salary)" Format="C0" Sortable
            Aggregate="AggregateFunction.Sum" />
    </Columns>
</BbDataGrid>